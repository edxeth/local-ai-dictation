{
  "name": "local-ai-dictation-electrobun",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "electrobun dev",
    "dev": "electrobun dev --watch",
    "check": "bun build src/mainview/index.ts --target=browser --outdir .tmp-check/renderer && bun build src/bun/index.ts --target=bun --outdir .tmp-check/bun"
  },
  "dependencies": {
    "electrobun": "^1.15.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "rcedit": "^4.0.1",
    "typescript": "^5.9.3"
  }
}
