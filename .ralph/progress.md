- 2026-03-23 — Item: Windows-side staging from WSL to a drive-backed Windows desktop copy. Decisions: added `parakeet gui-stage` and reused `cmd.exe` + `wslpath` from WSL to stage `desktop/electrobun` under `%LOCALAPPDATA%\ParakeetDictation\staging`, ignoring transient build/install folders. Files changed: `src/parakeet/desktop.py`, `src/parakeet/cli.py`, `tests/test_desktop.py`, `.ralph/items.json`. Verification: `.venv/bin/python -m compileall transcriber.py src tests` ✅; `.venv/bin/python -m pytest -q` ✅ (43 passed); `cd desktop/electrobun && bun install && bun run check` ✅; `.venv/bin/python -m parakeet.cli gui-stage --json` ✅ produced `C:\Users\mysol\AppData\Local\ParakeetDictation\staging\...\desktop\electrobun`; `cmd.exe /v:on /c "cd /d C:\Users\mysol\AppData\Local\ParakeetDictation\staging\projects-67656700e576\desktop\electrobun && echo !CD! && if exist package.json echo PACKAGE_OK"` ✅ printed the drive-backed staged path and `PACKAGE_OK`. Next: use `gui-stage` as the handoff for the Windows packaging command item.
- 2026-03-23 — Item: Deterministic Windows packaging command from WSL. Decisions: added `parakeet gui-package`, which stages `desktop/electrobun`, discovers Windows Bun, prefers `powershell.exe -NoProfile` for drive-backed execution from WSL, and validates the produced installer/artifact paths before returning JSON. Files changed: `src/parakeet/desktop.py`, `src/parakeet/cli.py`, `tests/test_desktop.py`, `.ralph/items.json`. Verification: `.venv/bin/python -m compileall transcriber.py src tests` ✅; `.venv/bin/python -m pytest -q` ✅ (45 passed); `cd desktop/electrobun && bun install && bun run check` ✅; `.venv/bin/python -m parakeet.cli gui-package --json` ✅ produced `build/stable-win-x64/parakeet-desktop-Setup.exe` and `artifacts/stable-win-x64-parakeet-desktop-Setup.zip` from the staged Windows path. Next: use the packaged artifact to prove unattended launch diagnostics for the packaged Windows app.
- 2026-03-23 — Item: Packaged Windows app unattended launch diagnostics. Decisions: added startup diagnostics plus log emission inside the packaged app, a renderer-ready RPC handshake, and `parakeet gui-package-smoke`, which reinstalls the packaged app, launches it unattended, waits for renderer/RPC readiness, then requests a clean window close through the installed Windows process. Files changed: `src/parakeet/desktop.py`, `src/parakeet/cli.py`, `desktop/electrobun/src/bun/index.ts`, `desktop/electrobun/src/mainview/index.ts`, `tests/test_desktop.py`, `.ralph/items.json`. Verification: `.venv/bin/python -m compileall transcriber.py src tests` ✅; `.venv/bin/python -m pytest -q` ✅ (47 passed); `cd desktop/electrobun && bun install && bun run check` ✅; `.venv/bin/python -m parakeet.cli gui-package-smoke --json --timeout-seconds 120` ✅ staged, packaged, installed, launched, captured logs, reported `bunReady=true` + `rendererReady=true` + `rendererRpcReady=true`, and shut down with `shutdownReason=window-close`. Next: build on the packaged-app diagnostics/log path to add the explicit localhost automation surface for the next E2E item.
- 2026-03-23 — Item: Deterministic `parakeet bridge` E2E mode without microphone/model loading. Decisions: added env-guarded bridge E2E behavior via `PARAKEET_E2E_MODE`, `PARAKEET_E2E_TRANSCRIPT`, `PARAKEET_E2E_START_DELAY_MS`, and `PARAKEET_E2E_STOP_DELAY_MS`, keeping the existing HTTP routes/payload shape while returning deterministic recording/transcribing/history results from the real bridge process. Files changed: `src/parakeet/bridge.py`, `tests/test_bridge.py`, `.ralph/items.json`. Verification: `.venv/bin/python -m compileall transcriber.py src tests` ✅; `.venv/bin/python -m pytest -q tests/test_bridge.py -k 'e2e or build_bridge_controller_from_namespace_reads_e2e_env'` ✅ (2 passed); `.venv/bin/python -m pytest -q` ✅ (49 passed); `cd desktop/electrobun && bun install && bun run check` ✅; direct WSL verification with `PYTHONPATH=src PARAKEET_E2E_MODE=1 PARAKEET_E2E_TRANSCRIPT='deterministic transcript' PARAKEET_E2E_START_DELAY_MS=30 PARAKEET_E2E_STOP_DELAY_MS=40 .venv/bin/python -u -m parakeet.cli bridge --host 127.0.0.1 --port 40124` plus HTTP assertions over `/health`, `/session/start`, `/session/stop`, and `/session/clear-history` ✅. Next: use the new deterministic bridge mode as the backend for the localhost GUI automation surface item.
- 2026-03-23 — Item: Localhost-only GUI automation surface for packaged Windows E2E. Decisions: added an env-guarded Bun localhost automation server with state/action endpoints, routed Windows launcher env through `powershell.exe -NoProfile` so packaged app E2E env vars survive WSL interop, and added `parakeet gui-package-automation` for targeted verification. Files changed: `desktop/electrobun/src/bun/index.ts`, `src/parakeet/desktop.py`, `src/parakeet/cli.py`, `tests/test_desktop.py`, `.ralph/items.json`. Verification: `.venv/bin/python -m compileall transcriber.py src tests` ✅; `.venv/bin/python -m pytest -q` ✅ (51 passed); `cd desktop/electrobun && bun install && bun run check` ✅; `.venv/bin/python -m parakeet.cli gui-package-automation --json --timeout-seconds 120` ✅ read `/state`, invoked `show-window`, `tray/open`, and `quit`, and captured `automationReady=true` plus `shutdownReason=automation:quit`; regression check `.venv/bin/python -m parakeet.cli gui-package-smoke --json --timeout-seconds 120` ✅ with `shutdownReason=e2e-auto-exit`. Next: use the automation surface to drive the offline/online bridge recovery and main-window/tray/hotkey E2E items.
- 2026-03-23 — Item: Offline-to-online bridge recovery without restarting the packaged Windows GUI. Decisions: added a renderer automation snapshot RPC so unattended checks can assert visible UI state, then added `parakeet gui-package-bridge-recovery` to launch the packaged app against an unused bridge URL, verify the offline guidance in the renderer, start the real deterministic WSL bridge, and wait for reconnect before quitting. Files changed: `desktop/electrobun/src/bun/index.ts`, `desktop/electrobun/src/mainview/index.ts`, `src/parakeet/desktop.py`, `src/parakeet/cli.py`, `tests/test_desktop.py`, `.ralph/items.json`. Verification: `.venv/bin/python -m compileall transcriber.py src tests` ✅; `.venv/bin/python -m pytest -q` ✅ (53 passed); `cd desktop/electrobun && bun install && bun run check` ✅; `.venv/bin/python -m parakeet.cli gui-package-bridge-recovery --json --timeout-seconds 120` ✅ packaged, installed, showed `Disconnected` / `Bridge offline` plus bridge guidance, reconnected to the real WSL bridge without restart, and exited with `shutdownReason=automation:quit`. Next: reuse the renderer snapshot and packaged recovery flow for the main-window start/stop E2E item.
- 2026-03-23 — Item: Main-window start/stop against the deterministic WSL bridge. Decisions: added a renderer automation action for the primary toggle control and `parakeet gui-package-main-window` to package, install, launch against a real deterministic bridge, drive the main window button path, and assert visible transcript history. Files changed: `desktop/electrobun/src/bun/index.ts`, `desktop/electrobun/src/mainview/index.ts`, `src/parakeet/desktop.py`, `src/parakeet/cli.py`, `tests/test_desktop.py`, `.ralph/items.json`. Verification: `.venv/bin/python -m compileall transcriber.py src tests` ✅; `.venv/bin/python -m pytest -q` ✅ (55 passed); `cd desktop/electrobun && bun install && bun run check` ✅; `.venv/bin/python -m parakeet.cli gui-package-main-window --json --timeout-seconds 120` ✅ packaged, installed, recorded via the main window control, observed `recording` → `idle`, and confirmed `main window deterministic transcript` in visible history before `shutdownReason=automation:quit`. Next: reuse the same real-bridge flow for tray-toggle and hotkey end-to-end verification.
